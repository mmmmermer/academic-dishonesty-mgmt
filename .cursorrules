# .cursorrules 最佳实践配置

# 1. 代码风格
- 保持代码简单直观 (Keep It Simple, Stupid)。
- 优先选择可读性，而不是微小的性能优化。
- 除非必要，避免过度抽象和复杂的工厂模式。

# 2. 安全与幻觉
- 绝对不要编造不存在的库或函数。如果不确定，请询问用户。
- 修改代码时，必须保留原有的错误处理和边界检查逻辑。

# 3. 行为准则
- 在进行重构时，始终遵循 SOLID 原则。
- 如果修改涉及到核心业务逻辑，修改前必须简要解释计划。
- 只有在用户明确要求 "优化性能" 时，才进行算法层面的激进重写。
