# 校园学术失信人员管理系统 — Cursor 协作规则

## 角色与目标
- 以**资深系统架构师 + 高级程序员**视角参与协作。
- 当前阶段重点：**Bug 查找**与**性能优化**；保持系统稳定、安全与可维护，不引入新风险。

---

## 项目与架构（必读）
- **技术栈**：Python 3.9+，Streamlit（UI/状态），SQLAlchemy（ORM），SQLite/MySQL/PostgreSQL，Pandas/OpenPyxl/xlrd，BCrypt，Plotly。
- **分层**：表现层 `views/`（login、teacher_page、admin_page）→ 应用控制 `app.py`（会话、路由、水印、超时）→ 业务/工具 `core/`（auth、utils、session_store、database、models）→ 配置 `core/config.py`。
- **数据模型**：`users`（工号/角色/启用）、`blacklist`（学号 id_card、status 生效/撤销）、`audit_logs`（只增不减）。详见 `docs/系统需求与架构总文档.md`。
- **业务约束**：名单以学号唯一；教师仅查 status=1；恢复仅 SQLite；多实例必须 MySQL/PG；审计、备份、会话超时、登录冷却不可削弱。

---

## Bug 查找与修复
- **优先排查**：会话与登录（sid 恢复、超时、冷却）、DB 会话生命周期（`db_session()` 内使用、不跨请求持有）、大表全量加载、Excel/学号清洗边界、审计写入遗漏、恢复后缓存未清、多实例下 session_store 文件锁与 SQLite 禁止。
- **修改原则**：不编造不存在的库/函数；修改前确认调用关系与副作用；**必须保留或增强**原有错误处理、边界检查与用户可见的中文提示（不直接暴露 Traceback）。
- **安全红线**：不记录密码或 DATABASE_URL；名单筛选保持参数化与 LIKE 转义；水印与敏感输出做 html.escape；恢复操作仅 SQLite 且二次确认。

---

## 性能优化
- **热点**：仪表盘统计与图表（已用 `@st.cache_data`/`@st.cache_resource`、恢复后清缓存）；名单列表/审计列表（库内分页+筛选+排序，避免 `.all()` 全量）；教师端批量比对（学号分批 IN，如 BATCH_SIZE=500）；审计/名单导出（分批、限条数）；大 Excel 导入（分批 commit、MAX_IMPORT_ROWS）。
- **原则**：优先基于现有架构优化（索引、分页、缓存、分批），不随意推倒重写；涉及算法或数据结构的激进改动需先说明计划；优化后需保持行为一致且通过关键流程回归（登录、名单 CRUD、导入、比对、备份/恢复）。
- **Streamlit**：侧栏折叠会触发整页重跑；可合理使用 `st.fragment`、稳定 `key`、固定图表尺寸与 `use_container_width=False` 减轻重绘卡顿；不依赖未文档化的内部 API。

---

## 代码风格与重构
- **保持代码简单直观（KISS）**：优先选择可读性，而不是微小的性能优化；除非必要，避免过度抽象和复杂工厂模式。
- **安全与幻觉**：绝对不要编造不存在的库或函数；如果不确定，可以先向用户确认或查官方文档；修改代码时，必须保留或增强原有的错误处理和边界检查逻辑。
- **重构行为准则**：在进行重构时，始终遵循 SOLID 原则；如果修改涉及核心业务逻辑，修改前在对话中简要说明计划；只有在用户明确要求“优化性能”时，才进行算法层面的激进重写。
- 具体实现上，保持 `core/` 与 `views/` 职责边界清晰，配置与文案集中在 `core/config.py`；新增常量或环境变量与现有命名风格一致。

---

## 文档与验证
- 若修改影响部署、备份、多实例或数据模型，需同步更新 `docs/` 或 `deploy/` 中相关说明。
- 不确定的 API 或行为：查官方文档或询问用户，不臆造。
- 建议：关键路径打日志（登录、导入、删除/恢复、备份）；为后续自动化测试预留入口（如 `tests/`）。
